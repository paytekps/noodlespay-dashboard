import Link from 'next/link';

type Program = { id: string; display_name: string; bin_prefix: string; enabled: boolean };

export default function ClosedLoopProgramSummary({ programs }: { programs: Program[] }) {
  return <section className="mt-7 rounded-xl border bg-white p-5 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="text-lg font-semibold">Closed-loop card programs</h2><p className="mt-1 max-w-3xl text-sm text-gray-600">The combined terminal uses an enabled program&apos;s verified BIN to identify the card after Datecs returns a payment result. It displays the program name and records it in transaction reporting. BIN matching alone does not connect or authorize a provider account.</p></div><Link href="/dashboard/integrations" className="rounded border px-3 py-2 text-sm">Open integrations</Link></div>{programs.length ? <div className="mt-4 grid gap-2">{programs.map(program => <div key={program.id} className="flex flex-wrap items-center justify-between gap-3 rounded-lg border p-3"><div><span className="font-medium">{program.display_name}</span><span className="ml-3 font-mono text-sm text-gray-500">BIN {program.bin_prefix}</span></div><span className={`rounded-full px-2 py-1 text-xs ${program.enabled ? 'bg-green-100 text-green-800' : 'bg-gray-100 text-gray-600'}`}>{program.enabled ? 'Enabled for identification' : 'Disabled'}</span></div>)}</div> : <div className="mt-4 rounded-lg border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900"><strong>No combined closed-loop programs configured.</strong><p className="mt-1">The four fields previously shown on this page belonged to the legacy dashboard and were not configuring the combined terminal.</p></div>}</section>;
}
