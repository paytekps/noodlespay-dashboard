import Link from 'next/link';

const steps = [['1', 'Set the amount', 'Choose a default amount in the dashboard. Pro and Premium plans can offer additional on-device amount controls.'], ['2', 'Present the card', 'The cardholder taps a contactless card or inserts a chip card while the amount remains ready.'], ['3', 'See the result', 'The device processes the payment and the merchant can review activity and reporting in the dashboard.']];

export default function HowItWorksPage() {
  return <main className="mx-auto max-w-6xl px-6 py-20"><p className="text-sm font-bold uppercase tracking-widest text-emerald-700">How Gimml works</p><h1 className="mt-3 max-w-3xl text-5xl font-black tracking-tight">A payment experience designed to stay out of the way.</h1><p className="mt-5 max-w-2xl text-lg leading-8 text-gray-600">The amount on screen is always ready for a tap or insert. Optional controls let the merchant offer more choice without turning the device into a complicated register.</p><div className="mt-12 grid gap-5 md:grid-cols-3">{steps.map(([number,title,description]) => <div key={number} className="rounded-2xl border bg-white p-7"><div className="text-4xl font-black text-emerald-600">{number}</div><h2 className="mt-5 text-xl font-bold">{title}</h2><p className="mt-3 leading-7 text-gray-600">{description}</p></div>)}</div><div className="mt-12 rounded-2xl bg-gray-950 p-8 text-white"><h2 className="text-2xl font-bold">Built for your organization</h2><p className="mt-2 text-gray-300">Manage multiple merchants and devices, assign access by user type, and export transaction information from one dashboard.</p><Link href="/signup" className="mt-6 inline-block rounded-lg bg-emerald-400 px-5 py-3 font-bold text-gray-950">Get started</Link></div></main>;
}
