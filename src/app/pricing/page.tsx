import Link from 'next/link';

const plans = [{ name: 'Basic', description: 'A simple fixed amount that is always ready to accept a card.', features: ['Saved default amount', 'Tap or insert payments', 'Merchant reporting'] }, { name: 'Pro', description: 'More flexibility for merchants that need adjustable amounts.', features: ['Everything in Basic', 'On-device amount increase', 'Automatic amount reset'] }, { name: 'Premium', description: 'The fullest on-device experience for organizations offering suggested amounts.', features: ['Everything in Pro', 'Three preset amount choices', 'Full dashboard controls'] }];

export default function PricingPage() {
  return <main className="mx-auto max-w-6xl px-6 py-20"><div className="text-center"><p className="text-sm font-bold uppercase tracking-widest text-emerald-700">Plans</p><h1 className="mt-3 text-5xl font-black tracking-tight">Choose how much control appears on the device.</h1><p className="mx-auto mt-5 max-w-2xl text-lg text-gray-600">Final device and service pricing will appear here before checkout is enabled.</p></div><div className="mt-12 grid gap-5 lg:grid-cols-3">{plans.map((plan,index) => <div key={plan.name} className={`rounded-2xl border bg-white p-7 shadow-sm ${index===2?'border-emerald-500 ring-2 ring-emerald-100':''}`}><h2 className="text-2xl font-black">{plan.name}</h2><p className="mt-3 min-h-14 text-gray-600">{plan.description}</p><ul className="mt-6 space-y-3">{plan.features.map(feature => <li key={feature} className="flex gap-2"><span className="font-bold text-emerald-600">✓</span>{feature}</li>)}</ul><Link href={`/signup?plan=${plan.name.toLowerCase()}`} className="mt-8 block rounded-lg bg-gray-950 px-5 py-3 text-center font-bold text-white">Select {plan.name}</Link></div>)}</div></main>;
}
